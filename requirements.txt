# Host machine requirements (Windows)
# Install with:  pip install -r requirements.txt
#
# For Docker deployment the AI brain uses requirements.docker.txt instead.
# This file is for running everything locally WITHOUT Docker.

# Core ML
torch>=2.0.0
numpy>=1.24.0

# Windows system monitoring (host only — NOT available in Docker)
pywin32>=306
pynput>=1.7.6

# File watching
watchdog>=3.0.0

# Memory and embeddings
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2

# Encryption
cryptography>=41.0.0

# UI
rich>=13.7.0

# FastAPI (only needed if running api/server.py locally without Docker)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# System tray icon (optional — for tray_icon.py)
pystray>=0.19.0
Pillow>=10.0.0

# Misc
psutil>=5.9.0
gitpython>=3.1.0
apscheduler>=3.10.0
